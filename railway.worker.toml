[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./entrypoint.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.env]
PYTHONUNBUFFERED = "1"
SERVICE_MODE = "llm-worker"

